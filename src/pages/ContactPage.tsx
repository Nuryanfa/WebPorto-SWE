import { Contact } from '@/components/sections/Contact';

/**
 * Contact page — full transmission panel.
 */
export default function ContactPage() {
  return (
    <div className="pt-[var(--nav-height)]">
      <Contact />
    </div>
  );
}
